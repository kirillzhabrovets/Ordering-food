{
  "name": "food",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server -w dist/db.json"
  },
  "author": "Kirill",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.1"
  }
}
